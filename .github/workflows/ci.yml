name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  build-and-test:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Setup Zig
        uses: goto-bus-stop/setup-zig@v2
        with:
          version: 0.16.0

      - name: Cache Zig dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/zig
            .zig-cache
          key: ${{ runner.os }}-zig-${{ hashFiles('build.zig.zon') }}
          restore-keys: |
            ${{ runner.os }}-zig-

      - name: Install mlx-c
        run: |
          set -x
          brew update 2>&1 | tail -3
          brew install mlx-c 2>&1
          ls -la /opt/homebrew/lib/libmlxc.dylib 2>/dev/null || ls -la /usr/local/lib/libmlxc.dylib 2>/dev/null || echo "LIBRARY NOT FOUND"
          pkg-config --libs mlxc 2>&1 || echo "pkg-config failed"

      - name: Build
        run: |
          export LIBRARY_PATH="/opt/homebrew/lib:/usr/local/lib:$LIBRARY_PATH"
          export CPATH="/opt/homebrew/include:/usr/local/include:$CPATH"
          zig build -Doptimize=ReleaseFast

      - name: Run unit tests
        run: zig build test

      - name: Check formatting
        run: zig fmt --check src/
